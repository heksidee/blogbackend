{
  "name": "blogibackend",
  "version": "1.0.0",
  "description": "blogitehtävä",
  "main": "index.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "start": "cross-env NODE_ENV=production node index.js",
    "test": "cross-env NODE_ENV=test node --test",
    "lint": "eslint .",
    "build:ui": "rm -rf dist && cd '../OKT2/684 Full Stack Open - osa 4 - Express-sovellusten testaaminen, käyttäjänhallinta/blogilistasovellus/blogifrontend' && npm run build && cp -r build '../dist'",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "start:test": "cross-env NODE_ENV=test node --watch index.js"
  },
  "author": "HD",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cross-env": "^10.0.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "lodash": "^4.17.21",
    "mongoose": "^8.15.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.28.0",
    "@stylistic/eslint-plugin-js": "^4.4.1",
    "eslint": "^9.28.0",
    "globals": "^16.2.0",
    "nodemon": "^3.1.10",
    "prettier": "^3.6.2",
    "supertest": "^7.1.4"
  }
}
